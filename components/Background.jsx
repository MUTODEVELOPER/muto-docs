'use client'
import { useEffect, useRef } from 'react'
export default function Background(){const ref=useRef(null);useEffect(()=>{const c=ref.current,x=c.getContext('2d');let w,h,id;const pts=[];const resize=()=>{w=c.width=innerWidth;h=c.height=innerHeight};resize();for(let i=0;i<42;i++)pts.push({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.16,vy:(Math.random()-.5)*.16});const draw=()=>{x.clearRect(0,0,w,h);pts.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;x.fillStyle='rgba(215,255,0,.42)';x.fillRect(p.x,p.y,1.1,1.1)});id=requestAnimationFrame(draw)};draw();addEventListener('resize',resize);return()=>{cancelAnimationFrame(id);removeEventListener('resize',resize)}},[]);return <canvas ref={ref} className="background-canvas"/>}
